DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
# Generate four different values, one for each secret below, for example with:
# python3 -c "import secrets; print(secrets.token_urlsafe(48))"
BIOMETRIC_PEPPER=
OTP_PEPPER=
# Used only to generate deterministic, reproducible synthetic identities.
# It must be different from every factor pepper and API token.
EXPERIMENT_MASTER_SECRET=
CONTROLLER_API_TOKEN=
